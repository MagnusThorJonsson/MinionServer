
namespace MinionServer.Server
{
    public enum MessageType
    {
        // Connection actions
        ConnectionAccepted,

        // Player updates
        PlayerConnected,
        PlayerUpdate
    }
}
